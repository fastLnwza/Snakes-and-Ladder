# คู่มือการอัปโหลดเกมไปยัง itch.io

## ✅ คำตอบสั้นๆ: ใช่ คุณสามารถอัปโหลดจาก Mac ได้!

itch.io รองรับการอัปโหลดจากทุกแพลตฟอร์ม รวมถึง macOS

---

## 📦 ขั้นตอนที่ 1: เตรียมแพ็กเกจเกม

### วิธีที่ 1: ใช้สคริปต์อัตโนมัติ (แนะนำ)

1. **ให้สิทธิ์ execute กับสคริปต์:**
   ```bash
   chmod +x package_for_itch.sh
   ```

2. **รันสคริปต์เพื่อสร้างแพ็กเกจ:**
   ```bash
   ./package_for_itch.sh mac
   ```

3. **สคริปต์จะสร้างไฟล์:**
   - โฟลเดอร์ `itch_package_mac/` ที่มี `.app` bundle
   - ไฟล์ ZIP: `SnakesAndLadders3D_mac_v1.0.0.zip`

### วิธีที่ 2: ทำเองด้วยมือ

1. **Build โปรเจกต์:**
   ```bash
   cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
   cmake --build build --config Release
   ```

2. **สร้าง .app bundle:**
   ```bash
   mkdir -p "SnakesAndLadders3D.app/Contents/MacOS"
   mkdir -p "SnakesAndLadders3D.app/Contents/Resources"
   
   # Copy executable
   cp build/PacmanOpenGL "SnakesAndLadders3D.app/Contents/MacOS/SnakesAndLadders3D"
   chmod +x "SnakesAndLadders3D.app/Contents/MacOS/SnakesAndLadders3D"
   
   # Copy shaders
   cp -r build/shaders "SnakesAndLadders3D.app/Contents/Resources/"
   
   # Copy assets
   cp -r assets "SnakesAndLadders3D.app/Contents/Resources/"
   
   # Copy font
   cp build/pixel-game.regular.otf "SnakesAndLadders3D.app/Contents/Resources/" 2>/dev/null || \
   cp assets/fonts/pixel-game.regular.otf "SnakesAndLadders3D.app/Contents/Resources/"
   ```

3. **สร้าง Info.plist** (ดูในสคริปต์ package_for_itch.sh)

4. **Zip ไฟล์:**
   ```bash
   zip -r SnakesAndLadders3D_mac.zip SnakesAndLadders3D.app
   ```

---

## 🌐 ขั้นตอนที่ 2: อัปโหลดไปยัง itch.io

### 2.1 สร้างหน้าเกมใหม่

1. **ไปที่ itch.io และล็อกอิน:**
   - เปิด https://itch.io
   - คลิก "Create new project" หรือไปที่ https://itch.io/game/new

2. **กรอกข้อมูลเกม:**
   - **Project name**: Snakes and Ladders 3D (หรือชื่อที่คุณต้องการ)
   - **Project URL**: จะสร้างอัตโนมัติจากชื่อ
   - **Classification**: Game
   - **Kind of project**: Game
   - **Pricing**: เลือก Free หรือ Paid ตามต้องการ

### 2.2 อัปโหลดไฟล์เกม

1. **ไปที่แท็บ "Uploads":**
   - คลิก "New upload" หรือ "Add new file"

2. **เลือกแพลตฟอร์ม:**
   - เลือก "macOS" สำหรับ Mac build
   - (ถ้ามี Windows/Linux build ก็อัปโหลดเพิ่มได้)

3. **อัปโหลดไฟล์ ZIP:**
   - ลากไฟล์ `SnakesAndLadders3D_mac_v1.0.0.zip` ไปวาง
   - หรือคลิก "Choose file" แล้วเลือกไฟล์

4. **ตั้งค่าการดาวน์โหลด:**
   - **Filename**: ใช้ชื่ออัตโนมัติหรือเปลี่ยนได้
   - **Display name**: "macOS" หรือ "Mac"
   - **File type**: "Default" (itch.io จะตรวจจับอัตโนมัติ)

### 2.3 ตั้งค่าเพิ่มเติม

1. **Operating systems:**
   - ✅ macOS (สำหรับ Mac build)

2. **Architectures:**
   - ✅ 64-bit (x86_64) - สำหรับ Intel Mac
   - ✅ 64-bit (arm64) - สำหรับ Apple Silicon (M1/M2/M3)
   - *หมายเหตุ: ถ้าสร้าง universal binary จะรองรับทั้งสอง*

3. **Version:**
   - ใส่เวอร์ชัน เช่น "1.0.0"

4. **Release type:**
   - เลือก "Release" สำหรับเวอร์ชันที่เสร็จแล้ว
   - หรือ "Pre-release" สำหรับ beta

### 2.4 เพิ่มข้อมูลเกม

1. **แท็บ "Edit game":**
   - **Short description**: คำอธิบายสั้นๆ
   - **Full description**: ใส่รายละเอียดเกม (สามารถ copy จาก README.md)
   - **Tags**: เพิ่มแท็ก เช่น "3d", "board-game", "opengl", "multiplayer"

2. **Screenshots:**
   - อัปโหลดภาพจาก `assets/result/` เช่น:
     - Overview.png
     - Math_Game.png
     - Memory_Gameplay.png
     - ฯลฯ

3. **Cover image:**
   - ใช้ Overview.png หรือสร้างภาพปก

### 2.5 เผยแพร่

1. **คลิก "Save & view page"** เพื่อดูหน้าเกม
2. **คลิก "View page"** เพื่อดูหน้าสาธารณะ
3. **คลิก "Public"** เพื่อเผยแพร่ (หรือ "Draft" ถ้ายังไม่พร้อม)

---

## 💡 เคล็ดลับเพิ่มเติม

### สำหรับ macOS

1. **Code Signing (ถ้าต้องการ):**
   - ถ้าไม่ sign เกม ผู้ใช้ต้องคลิกขวา > Open เพื่อเปิดครั้งแรก
   - หรือไปที่ System Preferences > Security & Privacy > Allow

2. **Universal Binary:**
   - ถ้าต้องการรองรับทั้ง Intel และ Apple Silicon:
   ```bash
   # Build สำหรับทั้งสอง architecture
   cmake -S . -B build -DCMAKE_OSX_ARCHITECTURES="x86_64;arm64"
   cmake --build build
   ```

3. **ไฟล์ที่ต้องรวม:**
   - ✅ Executable
   - ✅ shaders/ (โฟลเดอร์ทั้งหมด)
   - ✅ assets/ (โฟลเดอร์ทั้งหมด)
   - ✅ Font file (pixel-game.regular.otf)
   - ⚠️ Dynamic libraries (SDL2_mixer.dylib) ถ้าไม่ได้ static link

### สำหรับหลายแพลตฟอร์ม

ถ้าต้องการอัปโหลดสำหรับ Windows และ Linux ด้วย:

1. **Windows:**
   - Build บน Windows หรือใช้ cross-compilation
   - อัปโหลด `.exe` + DLLs + assets

2. **Linux:**
   - Build บน Linux
   - อัปโหลด executable + assets
   - อาจต้องรวม library dependencies หรือบอกให้ผู้ใช้ติดตั้ง

---

## 🎯 Checklist ก่อนอัปโหลด

- [ ] Build เกมในโหมด Release
- [ ] ทดสอบรันเกมจาก build directory
- [ ] ตรวจสอบว่า assets ทั้งหมดถูก copy
- [ ] ตรวจสอบว่า shaders ถูก copy
- [ ] สร้าง .app bundle (macOS) หรือ executable (Windows/Linux)
- [ ] Zip ไฟล์ทั้งหมด
- [ ] ทดสอบ unzip และรันเกม
- [ ] เตรียม screenshots
- [ ] เขียนคำอธิบายเกม
- [ ] ตั้งค่า tags และ categories

---

## 📞 ปัญหาที่พบบ่อย

### Q: ผู้ใช้ไม่สามารถเปิดเกมได้ (macOS)
**A:** เกมอาจไม่ได้ code sign ให้ผู้ใช้:
1. คลิกขวาที่ .app > Open
2. หรือไปที่ System Preferences > Security & Privacy > Allow

### Q: เกมหา assets ไม่เจอ
**A:** ตรวจสอบว่า:
- assets/ อยู่ใน Resources/ (macOS) หรือข้าง executable (Windows/Linux)
- Path ในโค้ดใช้ relative path จาก executable

### Q: ไฟล์ใหญ่เกินไป
**A:** itch.io ให้พื้นที่ฟรี 1GB:
- ลบไฟล์ที่ไม่จำเป็น (เช่น build artifacts)
- ใช้ compression ที่ดีกว่า
- ลบ screenshots ที่ไม่ใช้

---

## 🎉 เสร็จแล้ว!

เมื่ออัปโหลดเสร็จ คุณจะได้ลิงก์หน้าเกมที่:
- https://yourusername.itch.io/snakes-and-ladders-3d

แชร์ลิงก์นี้ให้คนอื่นดาวน์โหลดและเล่นเกมของคุณได้เลย!

---

**คำถามเพิ่มเติม?** ดูเอกสาร itch.io: https://itch.io/docs/creators/getting-started

